* text=auto

# Shell scripts must keep LF endings (they run in the Linux server container; CRLF breaks the shebang).
*.sh text eol=lf

# Unity files
*.meta -merge=unityamlmerge -text
*.unity -merge=unityamlmerge -text
*.asset -merge=unityamlmerge -text
*.prefab -merge=unityamlmerge -text
